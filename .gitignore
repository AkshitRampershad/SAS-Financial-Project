__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/

# Generated at runtime by the data generators and model training - not
# committed, so the repo stays small and every clone regenerates a
# fresh copy on demand.
data/raw/
models/

.DS_Store
